# Server Configuration
PORT=3000
# `docker compose up` overrides this to `demo` for the api and migrate
# services regardless of what's set here — see docker-compose.yml. Anything
# other than the literal string 'production' leaves POST /v1/dev/token on.
NODE_ENV=development

# Postgres Container Credentials
POSTGRES_USER=sml_user
POSTGRES_PASSWORD=sml_password
POSTGRES_DB=sml_db

# Ports
API_HOST_PORT=8080
DBGATE_HOST_PORT=8070
POSTGRES_HOST_PORT=5434

# For infra only workflow
# docker compose up -d postgres dbgate
# npm run start:dev
DATABASE_URL=postgres://sml_user:sml_password@localhost:5434/sml_db

# --- auth ---
# Access tokens authorise a session; resolution tokens authorise one specific
# movement of money. They MUST use different secrets.
JWT_ACCESS_SECRET=dev-only-access-secret-change-me
JWT_RESOLUTION_SECRET=dev-only-resolution-secret-change-me
JWT_ACCESS_TTL_SECONDS=3600
JWT_RESOLUTION_TTL_SECONDS=120
JWT_ISSUER=http://localhost:8080/realms/send-money
